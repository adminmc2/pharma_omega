# Puro Omega - Asistente de Ventas RAG (Avanzado)
# Dependencias para versión con embeddings semánticos

# Framework de chat
chainlit>=1.0.0

# Cliente OpenAI (compatible con DeepSeek)
openai>=1.0.0

# Procesamiento numérico
numpy>=1.24.0

# Embeddings semánticos (recomendado para mejor calidad)
sentence-transformers>=2.2.0
torch>=2.0.0

# Variables de entorno
python-dotenv>=1.0.0
